#cloud-config
users:
  - default
  - name: runner
    groups: [sudo]
    sudo: ['ALL=(ALL) NOPASSWD:ALL']
    shell: /bin/bash
    system: true

write_files:
  - path: /opt/set-idle.sh
    owner: 'root:root'
    permissions: '0755'
    content: |
      #!/bin/bash
      echo "idle" > /tmp/actions-runner-state
  - path: /opt/set-active.sh
    owner: 'root:root'
    permissions: '0755'
    content: |
      #!/bin/bash
      echo "active" > /tmp/actions-runner-state
  - path: /opt/actions.runner.service.template
    owner: 'root:root'
    permissions: '0644'
    content: |
      [Unit]
      Description={{Description}}
      After=network.target
      
      [Service]
      ExecStart={{RunnerRoot}}/runsvc.sh
      ExecStartPost=/opt/set-idle.sh
      ExecStopPost=/usr/bin/sudo /usr/sbin/poweroff
      User={{User}}
      WorkingDirectory={{RunnerRoot}}
      Environment=ACTIONS_RUNNER_HOOK_JOB_STARTED=/opt/set-active.sh
      KillMode=process
      KillSignal=SIGTERM
      TimeoutStopSec=5min
      
      [Install]
      WantedBy=multi-user.target

runcmd:
  - curl -fsSL https://download.docker.com/linux/ubuntu/gpg | gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
  - echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null
  - apt-get update
  - apt-get install -y docker-ce docker-ce-cli containerd.io
  - systemctl enable docker
  - mkdir -p /home/runner/actions-runner
  - cd /home/runner/actions-runner
  - |
    ARCH=$(uname -m)
    if [ "$ARCH" = "x86_64" ]; then
      ARCH="x64"
    elif [ "$ARCH" = "aarch64" ]; then
      ARCH="arm64"
    else
      echo "Unsupported architecture: $ARCH"
      exit 1
    fi
    curl -o actions-runner-linux.tar.gz -L https://github.com/actions/runner/releases/download/v2.322.0/actions-runner-linux-${ARCH}-2.322.0.tar.gz
  - tar xzf actions-runner-linux.tar.gz
  - rm actions-runner-linux.tar.gz
  - ./bin/installdependencies.sh
  - chown -R runner:runner /home/runner/actions-runner
  - poweroff